// Definisi semua warna tema EPIC - Ecocultural Pattern Innovation Creator
import 'package:flutter/material.dart';

/// Semua warna yang digunakan di aplikasi EPIC.
/// Tidak boleh ada warna hardcode di luar file ini.
class AppColors {
  AppColors._();

  /// Merah Madura - tombol utama, aksi primer
  static const Color primary = Color(0xFFE63946);

  /// Emas Celurit - aksen, judul, highlight
  static const Color secondary = Color(0xFFFFB700);

  /// Hijau Berani - highlight, aksen alternatif
  static const Color accent = Color(0xFF2EC4B6);

  /// Hitam Batik - navbar, header, background gelap
  static const Color dark = Color(0xFF1C1C2E);

  /// Krem Garam - background utama
  static const Color light = Color(0xFFFFF8EE);

  /// Oranye Sate - badge & notifikasi
  static const Color pop = Color(0xFFFF6B35);

  /// Hijau sukses
  static const Color success = Color(0xFF06D6A0);

  /// Kuning warning
  static const Color warning = Color(0xFFFFB700);

  /// Merah error
  static const Color error = Color(0xFFEF233C);

  /// Warna teks utama (gelap)
  static const Color textPrimary = Color(0xFF1C1C2E);

  /// Warna teks sekunder (abu-abu)
  static const Color textSecondary = Color(0xFF666666);

  /// Warna teks di atas background gelap
  static const Color textOnDark = Color(0xFFFFFFFF);

  /// Warna abu untuk item tidak aktif
  static const Color inactive = Color(0xFF888888);

  /// Warna background card gelap
  static const Color cardDark = Color(0xFF2C2C3E);

  /// Warna langit untuk background home
  static const Color sky = Color(0xFF87CEEB);

  /// Warna tanah/ground
  static const Color ground = Color(0xFF8B7765);

  /// Warna border halus
  static const Color border = Color(0xFF333333);

  /// Transparansi overlay gelap
  static const Color overlayDark = Color(0x99000000);

  /// Transparansi overlay terang
  static const Color overlayLight = Color(0x33FFFFFF);
}
